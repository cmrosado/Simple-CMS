# mysql2 instead of sqlite3
# SQLite version 3.x
# gem install sqlite3
development:
  adapter: mysql
  encoding: utf8
  reconnect: false
  database: simple_cms_development
  username: simple@localhost
  password: secretpassword
  
# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
# for localhost
# production: socket: /tmp/mysql.sock
# production: reconnect: false
# development: socket: /var/run/mysqld/mysqld.sock
# test: socket: /tmp/mysql.sock

test:
  adapter: mysql
  encoding: utf8
  reconnect: false
  database: simple_cms_test
  username: root
  password:
  
production:
  adapter: mysql
  database: simple_cms_production
  username: alpine_challenge
  password: d23tJ6Dn
  host: totaline.com
  encoding: utf8
